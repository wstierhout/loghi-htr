# requirements-macos.txt
#
# Native macOS (Apple Silicon) environment: identical to requirements.txt
# except that CUDA-enabled TensorFlow is replaced by plain tensorflow plus
# the tensorflow-metal plugin, which exposes the Apple GPU as a TensorFlow
# GPU device. See docs/macos.md.

numpy==2.1.3
editdistance==0.8.1
tensorflow==2.19.0
tensorflow-metal
keras==3.9.2
matplotlib==3.10.3
opencv_python==4.11.0.86
tf_keras_vis==0.8.7
elasticdeform @ git+https://github.com/gvtulder/elasticdeform
blinker==1.9.0
fpdf==1.7.2
scikit-image==0.25.2
prometheus-client==0.21.1
xlsxwriter==3.2.3
six
Pillow==10.4.0
h5py==3.11.0
fastapi[standard]==0.115.12
uvicorn==0.34.2
typing-extensions==4.13.2
psutil==7.0.0
scipy==1.15.3
vgslify==0.14.0
python-bidi

# Word beam search decoder (needed by `utils/threading.py`)
# No prebuilt arm64 wheel exists; pip builds it from source (needs Xcode CLT).
word-beam-search @ git+https://github.com/githubharald/CTCWordBeamSearch

# Build helpers (ensure native extensions like word-beam-search compile cleanly)
setuptools>=65
wheel
pybind11
